# 프로젝트 인수인계

마지막 갱신: 2026-07-14

## 현재 상태

- 운영 기준 브랜치: `main`
- 리디자인 전 백업 브랜치: `backup/before-accommodation-redesign-20260714`
- 리디자인 전 백업 태그: `backup-before-accommodation-redesign-20260714`
- 현재 작업 브랜치: `feature/accommodation-platform-ui`
- 현재 작업 브랜치에는 숙소 UI 소스 변경이 아직 없습니다.

## 작업 환경

```bash
git clone https://github.com/chloe5027/HelloPraha.git
cd HelloPraha
npm install
npm run dev
```

개발 서버는 기본적으로 `http://localhost:8000`에서 실행됩니다.

`vite.config.js`는 개발 중 `/api` 요청을 `http://localhost:8090`으로 전달합니다. 로컬에서 실제 콘텐츠 데이터를 보려면 PocketBase 서버가 해당 주소에서 실행 중이거나, 개발용 API 주소를 별도로 설정해야 합니다.

## 프로젝트 구조

```text
src/
  views/                 페이지 단위 화면
    Main.vue             메인 페이지
    Stay.vue             숙소 페이지
    Reviews.vue          후기 페이지
    Tour.vue             투어 페이지
  components/
    Stay/                객실, 갤러리, 이벤트, 지도 등 숙소 하위 화면
  router/index.js        페이지 주소 정의
  style.css              전역 스타일
```

## PocketBase 관리 범위

관리자 페이지에서 수정할 수 있는 주요 컬렉션입니다.

| 컬렉션 | 용도 |
| --- | --- |
| `rooms` | 객실명, 설명, 이미지 |
| `gallery` | 숙소 갤러리 이미지 |
| `review` | 고객 후기 |
| `events` | 이벤트 및 프로그램 |
| `facilities` | 주변 시설 안내 |
| `route` | 오시는 길 안내 |

PocketBase 서버 설정과 데이터베이스 스키마는 현재 저장소에 포함되어 있지 않습니다. 서버 접근권한, 데이터 백업, 파일 업로드 백업 절차를 별도로 관리해야 합니다.

## 숙소 상세 UI 개편 방향

첨부된 숙소 플랫폼 화면을 참고하되, 다른 서비스의 디자인을 그대로 복제하지 않습니다.

1. 상단 이미지 갤러리와 숙소 요약 정보를 첫 화면에 배치합니다.
2. `정보`, `객실`, `후기`, `위치`, `이용안내`, `혜택` 탭을 고정 탐색으로 제공합니다.
3. 객실을 사진, 인원, 시설, 가격 또는 문의 버튼을 갖춘 카드로 표시합니다.
4. 모바일에는 하단 고정 `예약하기` 또는 `카카오톡 문의` 버튼을 둡니다.
5. 데스크톱에는 본문과 별도로 날짜·인원·예약 행동을 담은 고정 예약 패널을 둡니다.
6. 긴 소개글과 이용 규칙은 읽기 쉬운 섹션 또는 아코디언으로 나눕니다.

## 알려진 점검 항목

- `src/components/Stay/rooms.vue`는 객실 반복문 안에서 같은 객실 목록을 다시 반복하고 있어 중복 표시 가능성이 있습니다.
- 현재 숙소 페이지는 탭 중심 정보 페이지이므로, 예약 UI를 붙이기 전에 상세 상품 페이지 레이아웃으로 재구성하는 작업이 필요합니다.
- 일부 안내 문구와 메뉴는 코드에 직접 들어 있으므로, 자주 바뀌는 운영 문구는 PocketBase 데이터로 옮기는 것을 권장합니다.
- 실제 결제 기능은 프론트엔드만으로 구현하지 않고 결제 검증용 서버 로직과 예약 중복 방지 처리가 필요합니다.

## 기록 방식

작업을 시작하기 전 다음을 확인합니다.

```bash
git status
git pull
```

작업이 끝나면 변경 내용을 작은 단위로 커밋합니다.

```bash
git add <파일>
git commit -m "feat: 숙소 상세 상단 갤러리 추가"
```

커밋 후에는 작업 브랜치를 원격 저장소에 올리고 Pull Request를 만들어 검토합니다.

```bash
git push -u origin feature/브랜치이름
```

## 다음 작업

1. `Stay.vue`를 숙소 상세 상품 페이지 구조로 재설계합니다.
2. 갤러리, 숙소 요약, 고정 탭, 시설·혜택, 객실 카드 순서로 UI를 구현합니다.
3. 모바일과 데스크톱 화면을 모두 확인합니다.
4. 예약 기능은 UI 확정 후 PocketBase 컬렉션과 서버 검증 로직을 설계합니다.
